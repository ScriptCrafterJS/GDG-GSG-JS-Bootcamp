// (1) build the greeting function (explain each word you write):

function greeting(name) {
  // let num;
  console.log(name);
  // console.log("Hello " + name);
}

greeting();

// (2) build a function with return statement (calculate the sum of two numbers):
// 💡Note: sum() means: calling, Running, Invoking
// 💡Note: sum(2, 3) this expression will evaluate to 5

function sum(num1, num2) {
  return num1 + num2;
}

console.log(sum(2, 3));
